{
  "name": "fillscreen-image",
  "version": "0.3.0",
  "description": "Fill the screen with an image.",
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist --presets=latest",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kenneth Ko",
  "license": "GPL-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/ko/fillscreen-image"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-latest": "^6.24.1"
  }
}
